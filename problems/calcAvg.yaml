id: 9
title: CalcAvg
text: |
  Write a function called calcAvg that takes in an array of integers
  and returns the average of all of the integers in the array.

  This function should work with negative numbers!
  
  Refer to example below.
  
  calcAvg([5, 123, 12, 24]) =
  calcAvg([-123456]) =
code: |
  function calcAvg(arr) {
    return num;
  }

  assert(calcAvg(0) === 0);
  assert(calcAvg(123) === 6);
  assert(calcAvg(456) === 15);
  assert(calcAvg(789) === 24);
  assert(calcAvg(-123) === 4);
  assert(calcAvg(-456) === 7);
  alert('Passed all test cases!');
tests:
  - assert(calcAvg(0) === 0);
  - assert(calcAvg(123) === 6);
  - assert(calcAvg(456) === 15);
  - assert(calcAvg(789) === 24);
  - assert(calcAvg(-123) === 4);
  - assert(calcAvg(-456) === 7);
solution: |
  
timeout: 2500
